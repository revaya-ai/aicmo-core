# AI CMO, full build target

This has moved. The full product architecture, the runtime model (cron, agents,
Notion), the complete component map with build status and owners, and the open
gaps that block a working prototype now live in one canonical document at the
repo root:

[`ARCHITECTURE.md`](../ARCHITECTURE.md)
